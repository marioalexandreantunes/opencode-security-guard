# opencode-security-guard
An OpenCode plugin that protects the inference boundary by detecting, redacting, and blocking secrets such as API keys, tokens, passwords, and private keys before they reach the model.
